const NewDeviceSetupDownload = () => (
  <div>
    <p>
      Thanks! We'll send that to you as soon as it's ready. Next, download the Square Point of Sale
      app at the link below.
    </p>
    <br />
    <p>
      Once you've got the app installed and have your device code, use that to authenticate with the
      Square POS to begin taking orders.
    </p>
  </div>
);

export default NewDeviceSetupDownload;
