const NewDeviceSetupDownload = () => (
  <div>
    <p>
      Once you've got the app installed and have logged in with your device code, you're ready to
      start taking orders.
    </p>
  </div>
);

export default NewDeviceSetupDownload;
